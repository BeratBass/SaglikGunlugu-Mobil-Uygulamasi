import { registerRootComponent } from 'expo';
import App from './App';

// Use Expo Go for development. Remove `expo-dev-client` import because
// it's not installed in this managed project. If you want to use a custom
// dev client (to support native-only modules), install and configure
// `expo-dev-client` and EAS builds instead.

registerRootComponent(App);
